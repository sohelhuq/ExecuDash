/**
 * @fileoverview Firestore Security Rules for Firestudio.
 *
 * Core Philosophy:
 * This ruleset enforces a project-based ownership model. Each project acts as a security boundary,
 * and access to modules, Telegram bots, and finance data sync configurations is controlled by project membership.
 *
 * Data Structure:
 * - /projects/{projectId}: Stores project metadata.
 * - /projects/{projectId}/modules/{moduleId}: Stores module details for each project.
 * - /projects/{projectId}/telegram_bots/{telegramBotId}: Stores Telegram bot connection details.
 * - /projects/{projectId}/finance_data_syncs/{financeDataSyncId}: Stores finance data sync configurations.
 *
 * Key Security Decisions:
 * - All collections are scoped to a specific project.
 * - All write operations are restricted to authenticated users.
 * - List operations are restricted to authenticated users within a project.
 * - No schema validation is performed in this prototyping phase, except for ownership fields.
 *
 * Denormalization for Authorization:
 * - The `projectId` field is denormalized into the Module, TelegramBot, and FinanceDataSync documents
 *   to allow direct authorization checks without needing to perform parent document lookups.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Defines whether the user is signed in.
     */
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * @description Defines whether the user is the owner of the project.
     */
    function isOwner(projectId) {
      return isSignedIn() && request.auth.uid == projectId;
    }

    /**
     * @description Defines whether the user is the owner of the existing document.
     */
    function isExistingOwner(projectId) {
        return isOwner(projectId) && resource != null;
    }
    
    /**
     * @description Rules for the /projects collection.
     * @path /projects/{projectId}
     * @allow (create) - Authenticated user can create a project if the projectId matches their user ID.
     * @deny (create) - If the projectId does not match the authenticated user's ID.
     * @allow (read) - Any user can read project details.
     * @deny (update) - If the project does not exist.
     * @principle Enforces project ownership for writes and allows public reads.
     */
    match /projects/{projectId} {
      allow get, list: if true;
      allow create: if isOwner(projectId);
      allow update: if isExistingOwner(projectId);
      allow delete: if isExistingOwner(projectId);
    }

    /**
     * @description Rules for the /projects/{projectId}/modules collection.
     * @path /projects/{projectId}/modules/{moduleId}
     * @allow (create) - Authenticated user can create a module if the projectId matches the document's projectId.
     * @deny (create) - If the projectId does not match.
     * @allow (read) - Any user can read module details.
     * @deny (update) - If the module does not exist.
     * @principle Enforces project-based ownership for writes and allows public reads.
     */
    match /projects/{projectId}/modules/{moduleId} {
      allow get, list: if true;
      allow create: if isSignedIn() && request.resource.data.projectId == projectId;
      allow update: if isSignedIn() && request.resource.data.projectId == projectId && resource != null;
      allow delete: if isSignedIn() && resource.data.projectId == projectId && resource != null;
    }

    /**
     * @description Rules for the /projects/{projectId}/telegram_bots collection.
     * @path /projects/{projectId}/telegram_bots/{telegramBotId}
     * @allow (create) - Authenticated user can create a Telegram bot if the projectId matches the document's projectId.
     * @deny (create) - If the projectId does not match.
     * @allow (read) - Any user can read Telegram bot details.
     * @deny (update) - If the Telegram bot does not exist.
     * @principle Enforces project-based ownership for writes and allows public reads.
     */
    match /projects/{projectId}/telegram_bots/{telegramBotId} {
      allow get, list: if true;
      allow create: if isSignedIn() && request.resource.data.projectId == projectId;
      allow update: if isSignedIn() && request.resource.data.projectId == projectId && resource != null;
      allow delete: if isSignedIn() && resource.data.projectId == projectId && resource != null;
    }

    /**
     * @description Rules for the /projects/{projectId}/finance_data_syncs collection.
     * @path /projects/{projectId}/finance_data_syncs/{financeDataSyncId}
     * @allow (create) - Authenticated user can create a finance data sync if the projectId matches the document's projectId.
     * @deny (create) - If the projectId does not match.
     * @allow (read) - Any user can read finance data sync details.
     * @deny (update) - If the finance data sync does not exist.
     * @principle Enforces project-based ownership for writes and allows public reads.
     */
    match /projects/{projectId}/finance_data_syncs/{financeDataSyncId} {
      allow get, list: if true;
      allow create: if isSignedIn() && request.resource.data.projectId == projectId;
      allow update: if isSignedIn() && request.resource.data.projectId == projectId && resource != null;
      allow delete: if isSignedIn() && resource.data.projectId == projectId && resource != null;
    }
  }
}